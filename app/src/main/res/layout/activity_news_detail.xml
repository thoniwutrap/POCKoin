<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
                name="newsViewModel"
                type="com.mobile.tr.poc.koin.ui.news.presenter.NewsViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".ui.newsdetails.presenter.NewsDetailActivity">


        <ImageView
                android:scaleType="fitXY"
                app:layout_constraintTop_toTopOf="parent"
                android:img="@{newsViewModel.getNews(newsViewModel.newsPositionSelected).urlToImage}"
                android:layout_width="match_parent"
                android:layout_height="300dp"/>

        <TextView
                android:text="@{newsViewModel.getNews(newsViewModel.newsPositionSelected).title}"
                android:id="@+id/txtTitle"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>